import React from 'react';

const Footer = () => {
  return (
    <div className="footer">
      <p>HTML 转换工具 &copy; {new Date().getFullYear()} - 支持多种格式转换</p>
    </div>
  );
};

export default Footer;
